<progress>
  <phase>Phase 14a: Video format detection — runtime MP4/WebM selection</phase>
  <status>IN PROGRESS</status>
  <last_completed_step>EXPLORE</last_completed_step>
  <next_step>THINK HARD</next_step>
  <files_read_this_session>CLAUDE.md, progress.txt, js/config.js, js/state.js, js/video/recorder.js, js/state-management.js, js/animation/engine.js, js/validation/validations.js</files_read_this_session>
  <plan_summary>
    Replace CONFIG.VIDEO_MIME_TYPE (single string) with CONFIG.VIDEO_MIME_PRIORITY (ordered array).
    startRecording() iterates the list and picks the first supported MIME via isTypeSupported().
    Add APP_STATE.resolvedVideoMime to store the chosen MIME. downloadVideo() reads it to set the
    correct Blob type and file extension (.mp4 or .webm). resetState() clears resolvedVideoMime.
    Files: config.js, state.js, recorder.js, state-management.js, validations.js.
  </plan_summary>
  <blockers>none</blockers>
</progress>