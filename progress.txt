<progress>
  <phase>Phase 14b: Recording canvas — target crop + watermark</phase>
  <status>IN PROGRESS</status>
  <last_completed_step>EXPLORE</last_completed_step>
  <next_step>WRITE VALIDATIONS FIRST</next_step>
  <files_read_this_session>CLAUDE.md, progress.txt, js/config.js, js/state.js, js/video/recorder.js, js/state-management.js, js/animation/engine.js, js/validation/validations.js</files_read_this_session>
  <plan_summary>
    Create a hidden off-screen recording canvas (size×size) in startReveal(). Record that canvas
    instead of the display canvas. Each frame in animationLoop(), copy just the target region from
    the display canvas to the recording canvas, then draw a watermark. Add CONFIG keys for watermark
    (text, font size ratio, opacity, padding ratio). Add APP_STATE.recordingCanvas field.
    Files: config.js, state.js, engine.js, state-management.js, validations.js.
  </plan_summary>
  <blockers>none</blockers>
</progress>
