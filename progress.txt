<progress>
  <phase>Phase 7: Default Image Selection by Luminance Histogram Similarity</phase>
  <status>IN PROGRESS</status>
  <last_completed_step>THINK HARD</last_completed_step>
  <next_step>WRITE VALIDATIONS FIRST</next_step>
  <files_read_this_session>index.html, progress.txt, defaultImages/ directory listing</files_read_this_session>
  <plan_summary>
    Replace the procedural random target generator in "fate" mode with a system that loads
    15 default images from defaultImages/, computes luminance histograms for each and the
    source image, then picks the most similar match using histogram intersection distance.
    On-demand loading: images are fetched only when Reveal is clicked. Procedural generators
    remain as fallback if image loading fails (e.g. file:// CORS or missing files).
    Changes touch: CONFIG (add DEFAULT_IMAGE_PATHS, HISTOGRAM_BINS), the "fate" branch inside
    the Reveal button handler, and a new "Default Image Matching" section with functions for
    histogram computation, comparison, and image loading. The selection remains a mystery to
    the user — no UI indication of which image was chosen.
  </plan_summary>
  <blockers>none</blockers>
</progress>