<progress>
  <phase>Phase 8: Compact Setup Screen with Custom Dropdowns</phase>
  <status>IN PROGRESS</status>
  <last_completed_step>EXPLORE</last_completed_step>
  <next_step>THINK HARD</next_step>
  <files_read_this_session>index.html, progress.txt</files_read_this_session>
  <plan_summary>
    Redesign the setup screen to be more compact. Replace Quality and Animation Pattern
    button groups with custom-styled CRT-aesthetic dropdowns. Replace Hidden Image toggle
    buttons with a dropdown ("I'm feeling lucky" / "Luck is not my way"). All three controls
    sit in a single horizontal row below the photo preview. When "Luck is not my way" is
    selected, a file upload area with optional preview toggle appears below the row. Quality
    default stays 512x512, pattern default changes to luminance-ordered. On mobile the three
    dropdowns stack vertically. Touches: CSS (new .custom-select styles, update .setup-section,
    update responsive), HTML (replace setup sections with row of dropdowns), JS (replace
    initOptionGroup listeners with change handlers on select elements, update default pattern
    in APP_STATE). Existing initOptionGroup function and .option-btn styles can remain for
    any other uses but the setup section HTML/CSS is rewritten.
  </plan_summary>
  <blockers>none</blockers>
</progress>
